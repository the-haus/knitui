export const meta = {
  title: "YearPicker",
  description: "`YearPicker` is the inline year-selection picker — a `Calendar` pinned to the decade level (`minLevel=\"decade\"`). Selects years in `default` / `range` / `multiple` modes; values are `YYYY-MM-DD` strings normalised to Jan 1 of the year.",
};

<ComponentHeader id="dates/year-picker" />

{/* scaffolded: replace this comment with real prose */}

## Playground

<Playground id="dates/year-picker" />

<Examples id="dates/year-picker" />

<PropsTable id="dates/year-picker" />
