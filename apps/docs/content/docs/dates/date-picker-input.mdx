export const meta = {
  title: "DatePickerInput",
  description: "`DatePickerInput` is the input-trigger day picker: an `InputBase` trigger that opens a `DatePicker` in a popover (or modal). Pressing a day fills the trigger with the formatted value (`valueFormat`, default `'MMMM D, YYYY'`). `type=\"range\"` renders `start – end` and only commits once both ends are picked. Supports `label`, `description`, `placeholder`, `error`, `required`, and `clearable`.",
};

<ComponentHeader id="dates/date-picker-input" />

{/* scaffolded: replace this comment with real prose */}

## Playground

<Playground id="dates/date-picker-input" />

<Examples id="dates/date-picker-input" />

<PropsTable id="dates/date-picker-input" />
