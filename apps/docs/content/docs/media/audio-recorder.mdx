export const meta = {
  title: "AudioRecorder",
  description: "A hybrid, cross-platform audio recorder. On web it drives the browser `MediaRecorder` (+ Web Audio metering); on React Native it drives the `expo-audio` recorder. Both share one controller contract and the same kit-built chrome.",
};

<ComponentHeader id="media/audio-recorder" />

{/* scaffolded: replace this comment with real prose */}

<Callout kind="platform">
  `@knitui/media` owns a single shared player element per medium, teleported into
  whichever surface is active — so examples on this page play one at a time.
</Callout>

## Playground

<Playground id="media/audio-recorder" />

<Examples id="media/audio-recorder" />

<PropsTable id="media/audio-recorder" />
