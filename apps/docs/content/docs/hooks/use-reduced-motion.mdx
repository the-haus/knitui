export const meta = {
  title: "useReducedMotion",
  description: "Whether the user prefers reduced motion (web) — port of Mantine's `useReducedMotion`. Tracks the `prefers-reduced-motion` media query; SSR-safe (the server snapshot is `initialValue`).",
};

<HookReference slug="use-reduced-motion" />
