export const meta = {
  title: "useListState",
  description: "Manage array state with a rich set of immutable handlers — port of Mantine's `useListState`. Every handler returns a new array, so the state is safe to use as a dependency / memo input.",
};

<HookReference slug="use-list-state" />
