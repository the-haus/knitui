export const meta = {
  title: "useListState",
  description: "Manage array state with a rich set of immutable handlers — port of Mantine's `useListState`. Every handler returns a new array, so the state is safe to use as a dependency / memo input. Pure React, so identical on web and native.",
};

<HookReference slug="use-list-state" />
