export const meta = {
  title: "useAppState",
  description: "Coarse foreground/background state (web) — maps the Page Visibility API onto the cross-platform `AppVisibility` union. `document.hidden` → `background`, otherwise `active`. SSR-safe. The `use-app-state.native` sibling uses React Native's `A",
};

<HookReference slug="use-app-state" />
