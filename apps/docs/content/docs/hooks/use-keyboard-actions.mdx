export const meta = {
  title: "useKeyboardActions",
  description: "Keyboard/accessibility activation for an interactive host that is NOT a native `<button>` — web implementation.",
};

<HookReference slug="use-keyboard-actions" />
