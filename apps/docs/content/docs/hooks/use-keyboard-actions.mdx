export const meta = {
  title: "useKeyboardActions",
  description: "Keyboard/accessibility activation for an interactive host that is NOT a native `<button>` — web implementation. Returns `{ tabIndex, onKeyDown }` to spread onto the host so it is focusable and the configured {@link KeyboardActions} fire fro",
};

<HookReference slug="use-keyboard-actions" />
