export const meta = {
  title: "useMounted",
  description: "`false` during the initial render, `true` after mount — port of Mantine's `useMounted`. Useful for deferring client-only / post-mount UI. Pure React, so identical on web and native.",
};

<HookReference slug="use-mounted" />
