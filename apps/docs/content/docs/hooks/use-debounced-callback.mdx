export const meta = {
  title: "useDebouncedCallback",
  description: "Debounce a callback — port of Mantine's `useDebouncedCallback`. The returned function keeps a stable identity, always calls the latest `callback` (via `useCallbackRef`), and carries `cancel` / `flush`. The pending timer is cleared on unmoun",
};

<HookReference slug="use-debounced-callback" />
