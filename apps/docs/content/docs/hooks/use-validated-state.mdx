export const meta = {
  title: "useValidatedState",
  description: "Track a value alongside its validation result and the last valid value — port of Mantine's `useValidatedState`. Pure React, so identical on web and native.",
};

<HookReference slug="use-validated-state" />
