export const meta = {
  title: "useQueue",
  description: "A bounded queue that keeps `limit` items visible and holds the overflow — port of Mantine's `useQueue`. Pure React state, so identical on web and native.",
};

<HookReference slug="use-queue" />
