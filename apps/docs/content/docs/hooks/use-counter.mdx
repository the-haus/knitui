export const meta = {
  title: "useCounter",
  description: "Integer counter state with optional bounds — port of Mantine's `useCounter`. Returns `[count, handlers]`. Pure React state, so identical on web and native.",
};

<HookReference slug="use-counter" />
