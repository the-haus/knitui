export const meta = {
  title: "useElementSize",
  description: "Measure an element's content box, kept in sync via `ResizeObserver` (web). Returns a `ref` to attach, the measured `width`/`height`, and an empty `rootProps` (web drives measurement through the ref). The `use-element-size.native` sibling me",
};

<HookReference slug="use-element-size" />
