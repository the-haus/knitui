export const meta = {
  title: "useClipboard",
  description: "Copy-to-clipboard state — port of Mantine's `useClipboard`. Guarded for native/SSR: when `navigator.clipboard` is unavailable it records an `error` instead of assuming a global.",
};

<HookReference slug="use-clipboard" />
