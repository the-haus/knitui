export const meta = {
  title: "useInterval",
  description: "Declarative `setInterval` with start/stop/toggle and an `active` flag — port of Mantine's `useInterval`.",
};

<HookReference slug="use-interval" />
