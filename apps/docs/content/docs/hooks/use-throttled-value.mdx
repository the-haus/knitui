export const meta = {
  title: "useThrottledValue",
  description: "Throttle a rapidly-changing value — port of Mantine's `useThrottledValue`. The returned value updates at most once per `wait` ms (leading + trailing). Pure timers, so identical on web and native — handy for `ColorPicker` drag or scroll-driv",
};

<HookReference slug="use-throttled-value" />
