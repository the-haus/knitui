export const meta = {
  title: "useThrottledValue",
  description: "Throttle a rapidly-changing value — port of Mantine's `useThrottledValue`. The returned value updates at most once per `wait` ms (leading + trailing).",
};

<HookReference slug="use-throttled-value" />
