export const meta = {
  title: "useSetState",
  description: "Object state with shallow partial merge — port of Mantine's `useSetState`. `setState({ a })` merges `a` into the current state (class-component `setState` semantics).",
};

<HookReference slug="use-set-state" />
