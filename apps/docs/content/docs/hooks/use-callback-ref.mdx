export const meta = {
  title: "useCallbackRef",
  description: "Wrap a callback so its identity stays stable across renders while always invoking the latest version — port of Mantine's `useCallbackRef`. Lets event handlers and timers avoid stale closures without re-subscribing. This is the \"always-lates",
};

<HookReference slug="use-callback-ref" />
