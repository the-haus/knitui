export const meta = {
  title: "useCallbackRef",
  description: "Wrap a callback so its identity stays stable across renders while always invoking the latest version — port of Mantine's `useCallbackRef`.",
};

<HookReference slug="use-callback-ref" />
