export const meta = {
  title: "usePagination",
  description: "Pagination state + the classic truncated page range with `'dots'` — port of Mantine's `usePagination`. Controlled/uncontrolled via `useUncontrolled`. Pure computation, so identical on web and native.",
};

<HookReference slug="use-pagination" />
