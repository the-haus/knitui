export const meta = {
  title: "useDebouncedValue",
  description: "Debounce a rapidly-changing value — port of Mantine's `useDebouncedValue`. Returns `[debounced, cancel]`; `debounced` updates `wait` ms after `value` stops changing. `leading` emits the first change immediately. Pure timers, so identical on",
};

<HookReference slug="use-debounced-value" />
