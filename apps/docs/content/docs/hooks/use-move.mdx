export const meta = {
  title: "useMove",
  description: "Track pointer dragging over an element and report the normalized `[0, 1]` position. Web implementation — mirrors Mantine's `useMove`.",
};

<HookReference slug="use-move" />
