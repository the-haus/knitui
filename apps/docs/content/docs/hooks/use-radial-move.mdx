export const meta = {
  title: "useRadialMove",
  description: "Track pointer dragging over a circular control and report the angle (0–359) under the pointer. Web implementation — mirrors Mantine's `useRadialMove`.",
};

<HookReference slug="use-radial-move" />
