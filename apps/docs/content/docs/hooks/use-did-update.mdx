export const meta = {
  title: "useDidUpdate",
  description: "Like `useEffect`, but skips the initial mount — port of Mantine's `useDidUpdate`. Runs `fn` only when `dependencies` change after the first render.",
};

<HookReference slug="use-did-update" />
