export const meta = {
  title: "useForceUpdate",
  description: "Force a re-render imperatively — port of Mantine's `useForceUpdate`. Returns a stable function that bumps an internal counter. Pure React, so identical on web and native.",
};

<HookReference slug="use-force-update" />
