export const meta = {
  title: "useThrottledCallback",
  description: "Throttle a callback — port of Mantine's `useThrottledCallback`. The returned function has a stable identity and always calls the latest `callback`. Pure timers, so identical on web and native — handy for high-frequency events (drag, scroll)",
};

<HookReference slug="use-throttled-callback" />
