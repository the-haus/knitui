export const meta = {
  title: "useThrottledCallback",
  description: "Throttle a callback — port of Mantine's `useThrottledCallback`. The returned function has a stable identity and always calls the latest `callback`.",
};

<HookReference slug="use-throttled-callback" />
