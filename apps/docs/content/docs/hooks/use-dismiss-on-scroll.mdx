export const meta = {
  title: "useDismissOnScroll",
  description: "Close an open overlay when the page scrolls (the idiomatic native behavior). Web variant: a **no-op**. On web a floating engine typically follows scroll cheaply via `scroll`/`resize` listeners, so the overlay tracks its target instead of cl",
};

<HookReference slug="use-dismiss-on-scroll" />
