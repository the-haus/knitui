export const meta = {
  title: "useIsFirstRender",
  description: "`true` on the first render, `false` afterwards — port of Mantine's `useIsFirstRender`. Updates during render (no effect), so it's correct even before effects flush. Pure React, so identical on web and native.",
};

<HookReference slug="use-is-first-render" />
