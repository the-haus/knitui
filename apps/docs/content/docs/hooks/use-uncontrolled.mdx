export const meta = {
  title: "useUncontrolled",
  description: "Controlled/uncontrolled state for an ARBITRARY value — port of Mantine's `useUncontrolled`. Generalizes the boolean-only `internal/use-toggle`: when `value` is provided the state is controlled (the setter just forwards to `onChange`); other",
};

<HookReference slug="use-uncontrolled" />
