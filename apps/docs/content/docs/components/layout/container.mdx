export const meta = {
  title: "Container",
  description: "Container is a centered, max-width content wrapper that mirrors Mantine's `Container`. `size` caps the max-width to a named breakpoint or an arbitrary value; `fluid` makes it span the full parent width, ignoring `size`. Centering is handled via automatic horizontal margins.",
};

<ComponentHeader id="components/layout/container" />

{/* scaffolded: replace this comment with real prose */}

## Playground

<Playground id="components/layout/container" />

<Examples id="components/layout/container" />

<PropsTable id="components/layout/container" />
