export const meta = {
  title: "Stack",
  description: "Stack is a vertical flex column built on `Box`. It mirrors Mantine's `Stack`: `gap` takes a Tamagui space token, `align` maps to `alignItems`, and `justify` maps to `justifyContent`.",
};

<ComponentHeader id="components/layout/stack" />

{/* scaffolded: replace this comment with real prose */}

## Playground

<Playground id="components/layout/stack" />

<Examples id="components/layout/stack" />

<PropsTable id="components/layout/stack" />
