export const meta = {
  title: "Paper",
  description: "Paper is a generic content surface built on `Box`. `shadow` (xs→xl) adds elevation, `radius` rounds the corners, and `withBorder` draws an outline. No border or shadow is shown by default.",
};

<ComponentHeader id="components/layout/paper" />

{/* scaffolded: replace this comment with real prose */}

## Playground

<Playground id="components/layout/paper" />

<Examples id="components/layout/paper" />

<PropsTable id="components/layout/paper" />
