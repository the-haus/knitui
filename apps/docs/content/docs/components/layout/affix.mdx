export const meta = {
  title: "Affix",
  description: "`Affix` pins its children to a fixed position on the viewport (or nearest filling ancestor on React Native). Pass a `position` object with `top`, `right`, `bottom`, and/or `left` offsets — `$`-space tokens (`'$md'`) or raw numbers are accepted. By default the layer renders through a `Portal` so it escapes `overflow:hidden` / transformed ancestors.",
};

<ComponentHeader id="components/layout/affix" />

{/* scaffolded: replace this comment with real prose */}

## Playground

<Playground id="components/layout/affix" />

<Examples id="components/layout/affix" />

<PropsTable id="components/layout/affix" />
