export const meta = {
  title: "Stepper",
  description: "Stepper guides users through a multi-step flow. Compose `Stepper.Step` children for each step and an optional `Stepper.Completed` for the finished state. `active` controls the current position; `onStepClick` enables navigation between steps. `orientation` flips the layout to vertical, `iconPosition` mirrors the bubble to the right, and `keepMounted` preserves step state across navigation.",
};

<ComponentHeader id="components/navigation/stepper" />

{/* scaffolded: replace this comment with real prose */}

## Playground

<Playground id="components/navigation/stepper" />

<Examples id="components/navigation/stepper" />

<PropsTable id="components/navigation/stepper" />
