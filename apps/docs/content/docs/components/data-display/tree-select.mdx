export const meta = {
  title: "TreeSelect",
  description: "TreeSelect renders a hierarchical tree of nodes inside a `Combobox` dropdown. Clicking a leaf selects it and closes the dropdown; clicking a branch toggles expansion. Supports uncontrolled and controlled modes, optional search filtering, and a clear button.",
};

<ComponentHeader id="components/data-display/tree-select" />

{/* scaffolded: replace this comment with real prose */}

## Playground

<Playground id="components/data-display/tree-select" />

<Examples id="components/data-display/tree-select" />

<PropsTable id="components/data-display/tree-select" />
