export const meta = {
  title: "InputBase",
  description: "InputBase composes `Input` + `Input.Wrapper` into a single component. It forwards all wrapper props (label, description, error) and all input props (variant, size, sections, etc.) through a single API surface.",
};

<ComponentHeader id="components/inputs/input-base" />

{/* scaffolded: replace this comment with real prose */}

## Playground

<Playground id="components/inputs/input-base" />

<Examples id="components/inputs/input-base" />

<PropsTable id="components/inputs/input-base" />
