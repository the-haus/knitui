export const meta = {
  title: "CloseButton",
  description: "CloseButton mirrors Mantine's `CloseButton`, built on `ActionIcon`. It renders a centered `✕` glyph by default and inherits all `ActionIcon` props except `loading` and `children` (children are appended after the icon, useful for a `VisuallyHidden` label). Accent comes from the `theme` prop + palette ramp.",
};

<ComponentHeader id="components/inputs/close-button" />

{/* scaffolded: replace this comment with real prose */}

## Playground

<Playground id="components/inputs/close-button" />

<Examples id="components/inputs/close-button" />

<PropsTable id="components/inputs/close-button" />
