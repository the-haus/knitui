export const meta = {
  title: "MultiSelect",
  description: "MultiSelect renders selected values as removable pills inside a `PillsInput`, backed by a `Combobox` dropdown. It supports controlled/uncontrolled values, search, grouped options, a max-values cap, and a clear button. Accent follows the active Tamagui theme via the palette ramp.",
};

<ComponentHeader id="components/inputs/multi-select" />

{/* scaffolded: replace this comment with real prose */}

## Playground

<Playground id="components/inputs/multi-select" />

<Examples id="components/inputs/multi-select" />

<PropsTable id="components/inputs/multi-select" />
