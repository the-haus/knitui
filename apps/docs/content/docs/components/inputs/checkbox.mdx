export const meta = {
  title: "Checkbox",
  description: "Checkbox is a cross-platform toggle control. `variant` sets how the active colour ramp is applied; `size` scales the box, icon and label together. Compose `Checkbox.Group` for multi-select lists, `Checkbox.Card` for card-style toggles, and `Checkbox.Indicator` for a read-only presentational box.",
};

<ComponentHeader id="components/inputs/checkbox" />

{/* scaffolded: replace this comment with real prose */}

## Playground

<Playground id="components/inputs/checkbox" />

<Examples id="components/inputs/checkbox" />

<PropsTable id="components/inputs/checkbox" />
