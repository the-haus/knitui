export const meta = {
  title: "Loader",
  description: "Loader is a multi-type loading indicator (`oval` | `dots` | `bars`). Accent color comes from the active theme ramp via the `theme` prop. Motion honours the user's `prefers-reduced-motion` preference and animates identically on web and native.",
};

<ComponentHeader id="components/display/loader" />

{/* scaffolded: replace this comment with real prose */}

## Playground

<Playground id="components/display/loader" />

<Examples id="components/display/loader" />

<PropsTable id="components/display/loader" />
