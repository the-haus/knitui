export const meta = {
  title: "Separator",
  description: "Separator renders a divider line — mirrors Mantine's `Divider`. Supports `variant` (solid/dashed/dotted), `size` (thickness), `orientation` (horizontal/vertical), and an optional `label` anchored left, center, or right.",
};

<ComponentHeader id="components/display/separator" />

{/* scaffolded: replace this comment with real prose */}

## Playground

<Playground id="components/display/separator" />

<Examples id="components/display/separator" />

<PropsTable id="components/display/separator" />
