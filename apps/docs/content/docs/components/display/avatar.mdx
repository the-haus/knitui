export const meta = {
  title: "Avatar",
  description: "Avatar is a circular display element built from `Box` + `Text`. It renders an image when `src` is provided, falls back to initials derived from `name`, or shows custom `children` as the placeholder. `autoColor` automatically assigns a theme accent by hashing the name. Use `Avatar.Group` to stack overlapping avatars.",
};

<ComponentHeader id="components/display/avatar" />

{/* scaffolded: replace this comment with real prose */}

## Playground

<Playground id="components/display/avatar" />

<Examples id="components/display/avatar" />

<PropsTable id="components/display/avatar" />
