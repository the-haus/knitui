export const meta = {
  title: "VisuallyHidden",
  description: "`VisuallyHidden` renders content that is invisible on screen but remains fully available to assistive technology (screen readers, VoiceOver, TalkBack). On web the standard `sr-only` clip technique is used; on native the node collapses to ~0 layout impact while staying in the accessibility tree. Use it to add screen-reader-only labels, descriptions, or announcements without affecting visual layout.",
};

<ComponentHeader id="components/display/visually-hidden" />

{/* scaffolded: replace this comment with real prose */}

## Playground

<Playground id="components/display/visually-hidden" />

<Examples id="components/display/visually-hidden" />

<PropsTable id="components/display/visually-hidden" />
