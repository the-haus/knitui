export const meta = {
  title: "Code",
  description: "Inline or block-level monospace code. Tint comes from the active theme ramp (`$color3` surface, `$color12` text), so the `theme` prop recolors it with no per-component logic. `block` switches from inline `<code>` to a full-width `<pre>`.",
};

<ComponentHeader id="components/typography/code" />

{/* scaffolded: replace this comment with real prose */}

## Playground

<Playground id="components/typography/code" />

<Examples id="components/typography/code" />

<PropsTable id="components/typography/code" />
