export const meta = {
  title: "Mark",
  description: "Mark renders highlighted inline text — a semantic `<mark>` on web with a tinted fill. The tint comes from the active theme ramp (`$color5` fill, `$color12` text), so the `theme` prop recolors it with no per-component logic. Inherits the full `Text` surface (`size`, `fw`, `truncate`, …).",
};

<ComponentHeader id="components/typography/mark" />

{/* scaffolded: replace this comment with real prose */}

## Playground

<Playground id="components/typography/mark" />

<Examples id="components/typography/mark" />

<PropsTable id="components/typography/mark" />
