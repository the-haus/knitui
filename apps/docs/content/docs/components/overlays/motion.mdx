export const meta = {
  title: "Motion",
  description: "Configurable, preset-driven animation. `useMotionPreset(name, { transition?, duration? })` resolves a named recipe (or an inline preset, or `false`) into spreadable `{ transition, enterStyle, exitStyle, animateOnly }` props. Spread them onto any frame and wrap it in `AnimatePresence` for enter + exit. Reduced motion is honoured automatically. Presets use cross-platform Tamagui shorthand transforms, so they render identically on web and native.",
};

<ComponentHeader id="components/overlays/motion" />

{/* scaffolded: replace this comment with real prose */}

## Playground

<Playground id="components/overlays/motion" />

<Examples id="components/overlays/motion" />

<PropsTable id="components/overlays/motion" />
