export const meta = {
  title: "HoverCard",
  description: "HoverCard is a compound overlay that opens a floating dropdown when the pointer enters `HoverCard.Target`. Compose `HoverCard` → `HoverCard.Target` + `HoverCard.Dropdown`. The dropdown is hover-driven (not value-controlled); use `initiallyOpened` to start open, and `openDelay`/`closeDelay` to tune the timing.",
};

<ComponentHeader id="components/overlays/hover-card" />

{/* scaffolded: replace this comment with real prose */}

## Playground

<Playground id="components/overlays/hover-card" />

<Examples id="components/overlays/hover-card" />

<PropsTable id="components/overlays/hover-card" />
