export const meta = {
  title: "Fill Extrusion Layer",
  description: "`FillExtrusionLayer` renders polygons as extruded 3D volumes. `fillExtrusionHeight` (and optional `fillExtrusionBase`) drive each shape's vertical extent — typically from a data-driven `height` property — and are best viewed with a tilted `Camera` (`pitch`/`bearing`). Shading responds to the map's `<Light>`. It must live inside a source and reference it via `source`.",
};

<ComponentHeader id="map/layers/fill-extrusion-layer" />

{/* scaffolded: replace this comment with real prose */}

<Callout kind="platform">
  Map views fill their container rather than sizing to content; every example here
  is given an explicit height.
</Callout>

## Playground

<Playground id="map/layers/fill-extrusion-layer" />

<Examples id="map/layers/fill-extrusion-layer" />

<PropsTable id="map/layers/fill-extrusion-layer" />
