export const meta = {
  title: "Line Layer",
  description: "`LineLayer` strokes `LineString` features. `lineCap` and `lineJoin` control end and corner shape, `lineDasharray` makes dashed strokes, and `lineWidth` (like every paint property) can be a constant or a zoom-driven expression for routes that thicken as you zoom in.",
};

<ComponentHeader id="map/layers/line-layer" />

{/* scaffolded: replace this comment with real prose */}

<Callout kind="platform">
  Map views fill their container rather than sizing to content; every example here
  is given an explicit height.
</Callout>

## Playground

<Playground id="map/layers/line-layer" />

<Examples id="map/layers/line-layer" />

<PropsTable id="map/layers/line-layer" />
