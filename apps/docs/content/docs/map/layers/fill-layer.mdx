export const meta = {
  title: "Fill Layer",
  description: "`FillLayer` paints the interior of `Polygon` features. `fillColor` and `fillOpacity` set the fill, `fillOutlineColor` adds a 1px crisp edge, and `fillColor` can be a data-driven expression to choropleth-shade regions by a property.",
};

<ComponentHeader id="map/layers/fill-layer" />

{/* scaffolded: replace this comment with real prose */}

<Callout kind="platform">
  Map views fill their container rather than sizing to content; every example here
  is given an explicit height.
</Callout>

## Playground

<Playground id="map/layers/fill-layer" />

<Examples id="map/layers/fill-layer" />

<PropsTable id="map/layers/fill-layer" />
