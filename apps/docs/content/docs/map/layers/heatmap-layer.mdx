export const meta = {
  title: "Heatmap Layer",
  description: "`HeatmapLayer` renders point density as a smooth, color-graded surface. `heatmapWeight` weights individual points, `heatmapRadius` controls the blur kernel, `heatmapIntensity` scales the overall signal, and `heatmapColor` maps the `heatmap-density` ramp (0 → 1) to colors. It must live inside a source and reference it via `source`.",
};

<ComponentHeader id="map/layers/heatmap-layer" />

{/* scaffolded: replace this comment with real prose */}

<Callout kind="platform">
  Map views fill their container rather than sizing to content; every example here
  is given an explicit height.
</Callout>

## Playground

<Playground id="map/layers/heatmap-layer" />

<Examples id="map/layers/heatmap-layer" />

<PropsTable id="map/layers/heatmap-layer" />
