export const meta = {
  title: "View Annotation",
  description: "`ViewAnnotation` anchors arbitrary React content at a coordinate. Unlike `Marker` it carries `title`/`snippet` metadata and selection callbacks (`onSelect`/`onDeselect`). On web the children render as a DOM overlay positioned by the map engine.",
};

<ComponentHeader id="map/markers-and-annotations/view-annotation" />

{/* scaffolded: replace this comment with real prose */}

<Callout kind="platform">
  Map views fill their container rather than sizing to content; every example here
  is given an explicit height.
</Callout>

## Playground

<Playground id="map/markers-and-annotations/view-annotation" />

<Examples id="map/markers-and-annotations/view-annotation" />

<PropsTable id="map/markers-and-annotations/view-annotation" />
