export const meta = {
  title: "MapView",
  description: "`Map` is the cross-platform container. The same JSX renders via `maplibre-gl` on web and `@maplibre/maplibre-react-native` on native — the public props are RN-first and never leak engine types. Children (`Camera`, sources, layers, markers) configure the map through context.",
};

<ComponentHeader id="map/map-view" />

{/* scaffolded: replace this comment with real prose */}

<Callout kind="platform">
  Map views fill their container rather than sizing to content; every example here
  is given an explicit height.
</Callout>

## Playground

<Playground id="map/map-view" />

<Examples id="map/map-view" />

<PropsTable id="map/map-view" />
