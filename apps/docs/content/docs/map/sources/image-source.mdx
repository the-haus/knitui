export const meta = {
  title: "Image Source",
  description: "`ImageSource` pins a single static image to four geographic corners. Pair it with a `RasterLayer` that targets the source `id` to render the image — useful for floorplans, historical maps, or rendered overlays georeferenced onto the map.",
};

<ComponentHeader id="map/sources/image-source" />

{/* scaffolded: replace this comment with real prose */}

<Callout kind="platform">
  Map views fill their container rather than sizing to content; every example here
  is given an explicit height.
</Callout>

## Playground

<Playground id="map/sources/image-source" />

<Examples id="map/sources/image-source" />

<PropsTable id="map/sources/image-source" />
