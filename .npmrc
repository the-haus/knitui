# React Native monorepos must resolve a single copy of react / react-native.
# A hoisted layout + explicit hoist patterns avoids the duplicate-React crash.
node-linker=hoisted
public-hoist-pattern[]=*react*
public-hoist-pattern[]=*react-native*
shamefully-hoist=false
